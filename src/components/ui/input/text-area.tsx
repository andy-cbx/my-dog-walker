import * as React from 'react';import { ChangeEventHandler } from 'react';type TextAreaProps = React.TextareaHTMLAttributes<HTMLTextAreaElement>;export const TextArea: React.FC<TextAreaProps> = ({	name,	value,	placeholder,	onChange,}) => {	return (		<>			<textarea				className={`font-secondaryLight flex h-[109px] w-full rounded-lg border border-white/70 bg-transparent pl-[20px] pt-[15px] text-white placeholder:text-white/70`}				name={name}				value={value || ''}				placeholder={placeholder}				onChange={onChange}			/>		</>	);};
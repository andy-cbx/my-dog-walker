import React from 'react';import { cn } from '@/utils';type CardProps = {	id?: string;	className?: string;	children: React.ReactNode;};export const Card = React.forwardRef<HTMLDivElement, CardProps>(	({ id, className, children }, ref) => {		return (			<div id={id} ref={ref} className={'drop-shadow-2xl'}>				<div					className={cn(						'flex w-full flex-col gap-4 rounded-[60px] border-2 border-primary-red bg-primary-yellow px-10 py-6 text-center font-secondary text-[25px] text-primary-red ',						className,					)}					style={{						filter: 'drop-shadow(4px 10px 0px var(--primary-red))',					}}				>					{children}				</div>			</div>		);	},);Card.displayName = 'Card';
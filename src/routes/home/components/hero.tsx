import LogoSvg from '@/assets/hero-logo.svg';import heroImg from '@/assets/home-hero.png';import { InfiniteScroll } from '@/features/infitine-scroll';const ScrollItem = ({ children }: { children: string }) => {    return (        <p className={'text-nowrap font-primary text-4xl text-white'}>            {children}        </p>    );};const Logo = () => {    return (        <div            className={                'absolute top-0 flex w-full flex-row gap-8 overflow-hidden pl-7 pt-7'            }        >            <img src={LogoSvg} alt={'logo'} />        </div>    );};const ScrollingText = () => {    return (        <div            className={                'absolute bottom-0 flex w-full flex-row gap-8 overflow-hidden pb-8'            }        >            <InfiniteScroll>                <div className={'flex w-full flex-row gap-8'}>                    <ScrollItem>• photo & video updates</ScrollItem>                    <ScrollItem>• small groups</ScrollItem>                    <ScrollItem>• matched sizing</ScrollItem>                    <ScrollItem>• free meet and greet</ScrollItem>                    <ScrollItem>• photo & video updates</ScrollItem>                    <ScrollItem>• photo & video updates</ScrollItem>                    <ScrollItem>• small groups</ScrollItem>                    <ScrollItem>• matched sizing</ScrollItem>                    <ScrollItem>• free meet and greet</ScrollItem>                    <ScrollItem>• photo & video updates</ScrollItem>                </div>            </InfiniteScroll>        </div>    );};export const HomeHero = () => {    return (        <div className={'flex w-full items-end justify-end'}>            <div className={'relative shadow-hero'}>                <img src={heroImg} alt={'hero-image'} className={''} />                <Logo />                <ScrollingText />            </div>        </div>    );};
import React from 'react';import { GiHamburgerMenu } from 'react-icons/gi';import { NavLink } from 'react-router';import logo from '@/assets/logo-new.svg';import {	Drawer,	DrawerContent,	DrawerDescription,	DrawerOverlay,	DrawerTitle,	DrawerTrigger,} from '@/components/ui/drawer';import { paths } from '@/config/paths';import { NavButton } from '@/features/header/components/nav-button';export const MobileNav = () => {	const [drawerIsOpen, setDrawerIsOpen] = React.useState(false);	return (		<div			className={`absolute right-0 flex w-full justify-end px-4 md:hidden`}		>			<Drawer open={drawerIsOpen}>				<DrawerTrigger asChild onClick={() => setDrawerIsOpen(true)}>					<button className="md:hidden">						<GiHamburgerMenu							className={'size-6 text-primary-yellow'}						/>					</button>				</DrawerTrigger>				<DrawerOverlay onClick={() => setDrawerIsOpen(false)} />				<DrawerContent					side="right"					className="flex flex-col gap-8 border-l-0 bg-primary-red pt-10 text-primary-yellow sm:max-w-60"				>					<DrawerDescription />					<div className={'flex w-full justify-center'}>						<NavLink							to={paths.home.getHref()}							onClick={() => setDrawerIsOpen(false)}						>							<img								id={'nav-logo'}								src={logo}								alt={'logo'}								className={'text-primary-red'}							/>						</NavLink>					</div>					<div className={'flex w-full flex-col items-center gap-4'}>						<NavLink							to={paths.about.getHref()}							onClick={() => setDrawerIsOpen(false)}						>							<NavButton								label={'ABOUT US'}								className={'text-primary-red'}							/>						</NavLink>						<NavLink							to={paths.blog.getHref()}							onClick={() => setDrawerIsOpen(false)}						>							<NavButton								label={'BLOG'}								className={'text-primary-red'}							/>						</NavLink>						<NavLink							to={paths.sitting.getHref()}							onClick={() => setDrawerIsOpen(false)}						>							<NavButton								label={'HOUSE SITTING'}								className={'text-primary-red'}							/>						</NavLink>						<NavLink							to={paths.book.getHref()}							onClick={() => setDrawerIsOpen(false)}						>							<NavButton								label={'ENQUIRE'}								className={'text-primary-red'}							/>						</NavLink>					</div>				</DrawerContent>			</Drawer>		</div>	);};
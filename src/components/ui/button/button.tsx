import React from 'react';import { cn } from '@/utils/cn';type ButtonProps = {    label?: string;    icon?: React.ReactNode;    children?: React.ReactNode;    colour?: 'green' | 'pink' | 'yellow';};export const Button = ({    label,    icon,    colour = 'green',    children,}: ButtonProps) => {    return (        <button            className={cn(                'w-60 h-16 rounded-[300px] pb-1',                colour === 'green' ? 'bg-nav-btn-green' : '',                colour === 'pink' ? 'bg-nav-btn-pink' : '',                colour === 'yellow' ? 'bg-nav-btn-yellow' : '',            )}        >            {label ? (                <p className={'font-primary text-3xl text-white'}>{label}</p>            ) : (                children            )}        </button>    );};
import { useGSAP } from '@gsap/react';import gsap from 'gsap';import ScrollTrigger from 'gsap/ScrollTrigger';import { useRef } from 'react';import heroImg from '@/assets/home/our-walks-hero.png';import SmallDogBody from '@/assets/home/small-dog-vectors/small-dog-body.svg?react';import SmallDogPaws from '@/assets/home/small-dog-vectors/small-dog-paws.svg?react';import { cn } from '@/utils';gsap.registerPlugin(useGSAP, ScrollTrigger);export const DogCardAnim = ({	delay = 0,	trigger,	start,	className,}: {	delay?: number;	trigger: string;	start: string;	className?: string;}) => {	const tl = useRef<GSAPTimeline>();	useGSAP(() => {		tl.current = gsap			.timeline({				defaults: { duration: 0.5 },				delay: delay,				scrollTrigger: {					trigger: trigger,					start: start,				},			})			.from('.small-brown-dog', {				y: 110,			})			.to(				'.small-dog-left-paw',				{					y: -35,				},				'-=0.2',			)			.to(				'.small-dog-right-paw',				{					y: -25,				},				'<',			)			.to(				['#paws'],				{					zIndex: 10,				},				'-=0.3',			)			.to(				['.small-dog-left-paw', '.small-dog-right-paw'],				{					y: 0,				},				'-=0.3',			)			.to('.small-dog-tongue', {				scale: 1.15,				duration: 0.4,				rotate: 7,				yoyo: true,				repeat: -1,				ease: 'none',			})			.to(				'.small-dog-left-brow',				{					y: -3,					scale: 1.1,					duration: 1,					repeatDelay: 1,					rotate: -7,					yoyo: true,					repeat: -1,					ease: 'none',				},				'<',			)			.to(				'.small-dog-right-brow',				{					y: -1,					x: -1,					scale: 1.1,					duration: 1,					repeatDelay: 1,					rotate: 4,					yoyo: true,					repeat: -1,					ease: 'none',				},				'<',			)			.to(				'.small-dog-ear-left',				{					duration: 1,					repeatDelay: 0.9,					rotate: 4,					yoyo: true,					repeat: -1,					ease: 'none',				},				'<',			)			.to(				'.small-dog-ear-right',				{					duration: 1,					repeatDelay: 0.9,					rotate: -4,					yoyo: true,					repeat: -1,					ease: 'none',				},				'<',			);		gsap.timeline({			yoyo: true,			repeatDelay: 3,			repeat: -1,			delay: 2,		})			.to('.dog-head', {				rotate: 20,				duration: 0.7,				transformOrigin: 'bottom center',			})			.to(				'.small-dog-brows',				{					y: -5,				},				'<',			)			.to(				'.dog-head',				{					rotate: 0,					duration: 1.5,					transformOrigin: 'bottom center',				},				'+=1',			)			.to('.small-dog-brows', { y: 0 }, '<');	});	return (		<div			className={cn(				'relative flex w-full justify-center md:justify-start',				className,			)}		>			<SmallDogBody				style={{					filter: 'drop-shadow(3px 6px 0px var(--primary-red))',				}}				className={'scale-50 md:scale-100'}			/>			<SmallDogPaws				id={'paws'}				className={'absolute top-0 scale-50 md:scale-100'}				style={{					filter: 'drop-shadow(3px 6px 0px var(--primary-red))',				}}			/>		</div>	);};export const DogAnimation = () => {	return (		<div className={'flex flex-1'}>			<div className={'relative mt-[-4.5rem] md:-mt-20'}>				<DogCardAnim					className={''}					trigger={'#our-walks-trigger'}					start={'100% bottom'}				/>				<div					className={						'relative z-[1] -mt-16 overflow-hidden rounded-[40px] border-x-2 border-t-2 border-primary-red md:-mt-8'					}					style={{						filter: 'drop-shadow(3px 6px 0px var(--primary-red))',					}}				>					<img						id={'our-walks-trigger'}						src={heroImg}						alt={''}						className={'size-full object-cover'}					/>				</div>			</div>		</div>	);};
import { useGSAP } from '@gsap/react';import gsap from 'gsap';import { NavLink } from 'react-router';import portrait from '@/assets/enquire-portrait-new.svg';import { paths } from '@/config/paths';gsap.registerPlugin(useGSAP);export const HomeEnquire = () => {	return (		<div className={'z-[2] flex flex-col'}>			<div className={'flex flex-row justify-between gap-10'}>				<div					className={						'flex flex-1 flex-col items-center gap-16 font-primary text-our-doggies-txt'					}				>					<h1						className={							'font-primary text-8xl font-black text-primary-yellow drop-shadow-xl'						}						style={{ WebkitTextStroke: '2px var(--primary-red)' }}					>						Think we&#39;re a great fit?					</h1>					<NavLink						to={paths.book.getHref()}						className={'flex items-center justify-center'}					>						<img src={portrait} alt={''} className={'w-full'} />					</NavLink>				</div>			</div>		</div>	);};
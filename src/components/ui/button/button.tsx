import React from 'react';import { cn } from '@/utils/cn';type ButtonProps = {    label?: string;    icon?: React.ReactNode;    children?: React.ReactNode;    colour?: 'green' | 'pink' | 'yellow';    className?: string;};export const Button = ({    className,    label,    icon,    colour = 'green',    children,}: ButtonProps) => {    return (        <button            className={cn(                'w-60 h-16 rounded-[300px] pb-1',                colour === 'green' ? 'bg-nav-btn-green' : '',                colour === 'pink' ? 'bg-nav-btn-pink' : '',                colour === 'yellow' ? 'bg-nav-btn-yellow' : '',                'font-primary text-3xl text-white',                className,            )}        >            {label ? <p className={''}>{label}</p> : children}        </button>    );};
import { Button } from '@/components/ui/button';export const HomeNavButtons = () => {    return (        <div            className={                'flex h-32 w-full items-center justify-center gap-16 border-b-2 border-b-white bg-nav-background'            }        >            <Button label={'gallery'} colour={'green'} />            <Button label={'house sitting'} colour={'pink'} />            <Button label={'book now'} colour={'yellow'} />        </div>    );};
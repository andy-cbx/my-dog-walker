import oops from '@/assets/oops.svg';const BlogRoute = () => {    return (        <section            className={                'flex flex-col items-center justify-center bg-primary-blue pt-32'            }        >            <img src={oops} alt={''} />        </section>    );};export default BlogRoute;
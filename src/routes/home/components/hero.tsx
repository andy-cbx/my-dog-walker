import LogoSvg from '@/assets/hero-logo.svg';import heroImg from '@/assets/home-hero-new.png';import { InfiniteScroll } from '@/features/infitine-scroll';const ScrollItem = ({ children }: { children: string }) => {	return (		<p className={'text-nowrap font-primary text-5xl text-primary-red'}>			{children}		</p>	);};const ScrollingText = () => {	return (		<div			className={				'relative z-[2] flex w-full flex-row gap-8 overflow-hidden border-y-2 border-y-primary-red bg-primary-yellow'			}		>			<InfiniteScroll>				<div className={'mr-8 flex w-full flex-row gap-8 py-8'}>					<ScrollItem>• photo & video updates</ScrollItem>					<ScrollItem>• small groups</ScrollItem>					<ScrollItem>• matched sizing</ScrollItem>					<ScrollItem>• free meet and greet</ScrollItem>					<ScrollItem>• photo & video updates</ScrollItem>					<ScrollItem>• photo & video updates</ScrollItem>					<ScrollItem>• small groups</ScrollItem>					<ScrollItem>• matched sizing</ScrollItem>					<ScrollItem>• free meet and greet</ScrollItem>					<ScrollItem>• photo & video updates</ScrollItem>				</div>			</InfiniteScroll>		</div>	);};export const HomeHero = () => {	return (		<div className={'flex w-full'}>			<div className={'w-full'}>				<img src={heroImg} alt={'hero-image'} className={'w-full'} />				<ScrollingText />			</div>		</div>	);};
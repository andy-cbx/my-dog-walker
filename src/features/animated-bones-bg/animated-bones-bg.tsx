import { useGSAP } from '@gsap/react';import gsap from 'gsap';import { useRef } from 'react';import bgBones from '@/assets/global/bg-bones.png';import { cn } from '@/utils';export const AnimatedBones = ({ className }: { className?: string }) => {	const containerRef = useRef<HTMLDivElement>(null);	const timeline = useRef<GSAPTimeline>();	useGSAP(() => {		const setupInfiniteMarqueeTimeline = () => {			gsap.set(containerRef.current, {				yPercent: 0,			});			timeline.current = gsap				.timeline({					defaults: { ease: 'none', repeat: -1 },				})				.to(containerRef.current, {					yPercent: -50,					duration: 50,				})				.set(containerRef.current, { yPercent: 0 });		};		setupInfiniteMarqueeTimeline();	});	return (		<div			className={cn(				'fixed left-0 top-0 z-0 h-screen w-screen overflow-hidden opacity-5',				className,			)}		>			<div ref={containerRef} className={''}>				<img src={bgBones} alt={''} className={'size-full'} />				<img src={bgBones} alt={''} className={'size-full'} />			</div>		</div>	);};